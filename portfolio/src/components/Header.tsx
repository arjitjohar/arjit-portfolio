import React from "react";
import NavBar from "./NavBar";

type Props = {};

const Header = (props: Props) => {
  return (
    <div className="">
      <NavBar />
    </div>
  );
};
export default Header;
